version: '3.8'

services:
  bot:
    build: .
    container_name: disknogamerz-bot
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config.json:/app/config.json
    environment:
      - PYTHONUNBUFFERED=1
